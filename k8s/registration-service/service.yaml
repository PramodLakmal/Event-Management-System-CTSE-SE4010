apiVersion: v1
kind: Service
metadata:
  name: registration-service
  labels:
    app: registration-service
    component: microservice
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
  selector:
    app: registration-service
