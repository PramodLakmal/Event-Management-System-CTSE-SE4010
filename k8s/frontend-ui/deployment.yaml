apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-ui
  labels:
    app: frontend-ui
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-ui
  template:
    metadata:
      labels:
        app: frontend-ui
        component: frontend
    spec:
      containers:
      - name: frontend-ui
        image: sliitprojectmail/event-sync-frontend-ui:e6c66a7e91c83a55e68acd94b1f5af194332368f
        imagePullPolicy: Always
        ports:
        - containerPort: 5173
        env:
        - name: VITE_API_GATEWAY_URL
          value: "/api"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
