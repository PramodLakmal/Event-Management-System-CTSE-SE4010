apiVersion: v1
kind: Service
metadata:
  name: frontend-ui
  labels:
    app: frontend-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5173
    protocol: TCP
  selector:
    app: frontend-ui
