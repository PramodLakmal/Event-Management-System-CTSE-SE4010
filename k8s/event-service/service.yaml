apiVersion: v1
kind: Service
metadata:
  name: event-service
  labels:
    app: event-service
    component: microservice
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
  selector:
    app: event-service
